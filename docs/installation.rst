============
Installation
============

At the command line::

    $ easy_install django-subcommand

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-subcommand
    $ pip install django-subcommand
